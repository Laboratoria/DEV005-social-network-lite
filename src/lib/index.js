// aqui exportaras las funciones que necesites

import { createPost, deletePost, editPost, getPosts, init, login,  } from "./services";
//import {registro} from "..components/src"
/*
export const register = (email, password) => new Promise((resolve, reject) => {
  registro(register, email, password)
}){
  // aqui tu codigo



  //console.log('Hola mundo!');
};
